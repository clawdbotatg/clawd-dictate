// Written to Secrets.swift by ios/gen-secrets.sh (gitignored) from
// ~/.config/clawd-dictate/env — HARNESS_URL, CAL_URL, SLOP_URL,
// DEEPGRAM_API_KEY, DOCS_CREDENTIAL. Nothing here is in the repo.
enum Secrets {
    static let deepgramKey = "…"
    static let docsCredential = "…"
    static let relay = "https://your-relay"        // HARNESS_URL
    static let calURL = "https://your-calendar"    // the 📅 key
    static let slopURL = "https://your-other-link" // the 💻 key
}
